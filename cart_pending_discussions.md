# 장바구니 기능 팀 협의 필요 사항

## 문서 목적

이 문서는 권오현 담당 장바구니 기능 중 현재 ERD와 기존 프로젝트만으로 확정할 수 없는 사항을 기록한다.

- 합의 전에는 관련 기능을 임의로 구현하지 않는다.
- 장바구니 CRUD와 독립 테스트는 확정된 ERD 범위에서 먼저 진행할 수 있다.
- 팀 회의 후 결정된 내용은 이 문서와 필요한 코드에 함께 반영한다.

## 1. 로그인 회원 UUID 연결

### 현재 상황

- 팀 ERD의 `customers.id`는 `uuid` 타입이다.
- 기존 로그인 화면은 `id01` 같은 문자형 로그인 아이디를 사용한다.
- 장바구니의 `carts.customer_id`는 `customers.id` UUID를 참조해야 한다.

### 협의할 내용

- 로그인 성공 후 회원 UUID를 어떤 방식으로 프런트엔드에 전달할지
- 기존 문자형 로그인 아이디를 계속 사용할지
- 별도의 `login_id` 컬럼을 ERD에 추가할지
- Supabase Auth 사용자 UUID를 `customers.id`로 사용할지

### 합의 전 임시 처리

- 독립 테스트에서는 테스트용 `customer_id` UUID를 사용한다.
- 기존 로그인·회원 파일은 수정하지 않는다.
- 실제 로그인 세션과 장바구니 연결은 보류한다.

### 결정 결과

- [ ] 결정 완료
- 결정 내용:
- 담당자:
- 반영 예정일:

## 2. 상품 판매 중지 상태

### 현재 상황

- `products`에는 `stock` 컬럼이 있다.
- 판매 상태를 나타내는 `status`, `is_active`, `deleted_at` 컬럼은 현재 ERD에 없다.
- 품절과 판매 중지를 구분할 수 없다.

### 협의할 내용

- 판매 중지 상태를 별도로 관리할지
- 상태 컬럼을 추가한다면 이름과 데이터 타입을 무엇으로 할지
- 판매 중지 상품을 기존 장바구니에 표시할지 자동 제거할지

### 합의 전 임시 처리

- `stock == 0`이면 품절로 처리한다.
- 상품 조회 결과가 없으면 삭제되었거나 존재하지 않는 상품으로 처리한다.
- 판매 중지 상품 구분은 구현하지 않고 코드에 `TODO` 주석을 남긴다.

### 결정 결과

- [ ] 결정 완료
- 결정 내용:
- 담당자:
- 반영 예정일:

## 3. 상품 삭제 시 장바구니 처리와 FK 정책

### 현재 상황

- `carts.product_id`는 `products.id`를 참조한다.
- 상품 삭제 시 사용할 FK 삭제 정책이 문서에 명시되어 있지 않다.

### 협의할 내용

- `CASCADE`: 상품 삭제 시 장바구니 항목도 함께 삭제
- `RESTRICT`: 장바구니에 담긴 상품은 삭제 제한
- `SET NULL`: 장바구니를 유지하고 상품 참조만 제거
- 삭제 상품명과 가격을 장바구니에 별도로 보존할 필요가 있는지

### 합의 전 임시 처리

- 상품 조회 결과가 없으면 삭제 상품으로 안내한다.
- 해당 항목은 수량 변경과 주문을 제한하고 장바구니 삭제만 허용한다.
- 실제 FK 정책 변경은 진행하지 않는다.

### 결정 결과

- [ ] 결정 완료
- 결정 내용:
- 담당자:
- 반영 예정일:

## 4. 주문 기능 전달 및 장바구니 삭제 시점

### 현재 상황

- 장바구니에서 선택한 상품의 주문용 데이터는 생성할 수 있다.
- 실제 주문 API 경로와 요청·응답 형식은 주문 담당자와 합의가 필요하다.
- 주문 성공 후 장바구니 항목을 어느 기능이 삭제할지 확정해야 한다.

### 제안 전달 형식

```json
{
  "customer_id": "회원 UUID",
  "items": [
    {
      "cart_id": "장바구니 UUID",
      "product_id": "상품 UUID",
      "product_name": "상품명",
      "quantity": 2,
      "price": 15000
    }
  ],
  "total_price": 30000
}
```

### 협의할 내용

- 주문 API 경로와 요청 형식
- 주문 직전에 가격과 재고를 다시 검사할 위치
- 주문 성공 후 선택 장바구니 항목을 삭제할 담당 기능
- 주문 실패 시 장바구니를 유지하는 방식
- 주문 과정에서 재고가 부족해졌을 때 반환할 오류 형식

### 합의 전 임시 처리

- 주문용 데이터 생성까지만 구현한다.
- 실제 주문 API 호출과 주문 완료 후 장바구니 삭제는 보류한다.

### 결정 결과

- [ ] 결정 완료
- 결정 내용:
- 담당자:
- 반영 예정일:

## 5. 실제 Supabase 통합 테스트 조건

### 확인할 사항

- [ ] `customers.id`가 UUID로 생성되어 있다.
- [ ] `products.id`가 UUID로 생성되어 있다.
- [ ] `products.stock`이 생성되어 있다.
- [ ] `carts` 테이블과 FK가 생성되어 있다.
- [ ] 통합 테스트용 회원 UUID가 준비되어 있다.
- [ ] 통합 테스트용 상품과 재고 데이터가 준비되어 있다.
- [ ] 팀 공통 Supabase 환경 변수가 설정되어 있다.

### 합의 전 임시 처리

- Schema, Service, Router의 독립 테스트를 우선 진행한다.
- 외부 기능은 테스트 대역 또는 테스트용 UUID 데이터로 검증한다.
- 실제 Supabase 통합 테스트는 위 조건이 충족된 후 진행한다.

## 팀 회의 후 반영 순서

1. 각 항목의 결정 결과를 이 문서에 기록한다.
2. ERD 변경 사항이 있으면 ERD와 `plan.md`를 먼저 갱신한다.
3. 상품·회원·주문 담당자가 자신의 기능에 결정 사항을 반영한다.
4. 장바구니의 보류 코드와 `TODO`를 결정 내용에 맞게 수정한다.
5. 실제 Supabase 환경에서 통합 테스트한다.
6. API 명세와 테스트 결과를 팀 문서 담당자에게 전달한다.

